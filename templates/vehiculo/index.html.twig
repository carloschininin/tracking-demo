{% extends 'base.html.twig' %}

{% block title %}Vehiculo index{% endblock %}

{% block body %}
    <h1>Vehiculo index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Matricula</th>
                <th>Marca</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for vehiculo in vehiculos %}
            <tr>
                <td>{{ vehiculo.id }}</td>
                <td>{{ vehiculo.matricula }}</td>
                <td>{{ vehiculo.marca }}</td>
                <td>
                    <a href="{{ path('app_vehiculo_show', {'id': vehiculo.id}) }}">show</a>
                    <a href="{{ path('app_vehiculo_edit', {'id': vehiculo.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_vehiculo_new') }}">Create new</a>
{% endblock %}
