{% extends 'base.html.twig' %}

{% block title %}Chofer{% endblock %}

{% block body %}
    <h1>Chofer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chofer.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ chofer.nombre }}</td>
            </tr>
            <tr>
                <th>Dni</th>
                <td>{{ chofer.dni }}</td>
            </tr>
            <tr>
                <th>Licencia</th>
                <td>{{ chofer.licencia }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chofer_index') }}">back to list</a>

    <a href="{{ path('app_chofer_edit', {'id': chofer.id}) }}">edit</a>

    {{ include('chofer/_delete_form.html.twig') }}
{% endblock %}
