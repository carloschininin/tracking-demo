{% extends 'base.html.twig' %}

{% block title %}Empleado Listado{% endblock %}

{% block body %}
    <h1>Empleado index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>#</th>
                <th>Apellido</th>
                <th>Nombre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for empleado in empleados %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>{{ empleado.apellido }}</td>
                <td>{{ empleado.nombre }}</td>
                <td>
                    <a href="{{ path('app_empleado_show', {'id': empleado.id}) }}">show</a>
                    <a href="{{ path('app_empleado_edit', {'id': empleado.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_empleado_new') }}">Create new</a>
{% endblock %}
