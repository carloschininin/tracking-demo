{% extends 'base.html.twig' %}

{% block title %}Cargamento{% endblock %}

{% block body %}
    <h1>Cargamento</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cargamento.id }}</td>
            </tr>
            <tr>
                <th>FechaPartida</th>
                <td>{{ cargamento.fechaPartida ? cargamento.fechaPartida|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cargamento_index') }}">back to list</a>

    <a href="{{ path('app_cargamento_edit', {'id': cargamento.id}) }}">edit</a>

    {{ include('cargamento/_delete_form.html.twig') }}
{% endblock %}
